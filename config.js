export const NUMBER_OF_SIMULATIONS = 200;

export const COHORT_SIZES = [10, 40, 75, 100, 150, 200, 350, 500];
export const NUMBER_OF_ITERATIONS = null; // set to null and each simulation will iterate until no initial members are left

export const BIAS_STRENGTH = 2.01; // how much of a boost in performance points the privileged group gets
export const ATTRITION_RATE = 0.15;
